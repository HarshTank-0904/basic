
INSTRUCTION:

SECTION A: FILTER TESTCASES

1. What if we add two filters on the same hook:
   - Filter A converts content to UPPERCASE
   - Filter B converts content to LOWERCASE
   Which output will finally appear?


2. What if we add two filters:
   - Both filters append text to the content
   What will the final content look like?


3. What if two filters have the SAME priority?
   In which order will they execute?


4. What if one filter does NOT return anything?
   What happens to the content?


5. What if a filter returns completely new content
   and ignores the original content?


6. What if a filter is removed BEFORE the hook runs?


7. What if a filter is removed AFTER it already ran?


8. What if the same filter function is added twice?


9. What if a filter contains a condition like
   is_single() or is_page()?


10. What if a filter echoes text instead of returning it?


11. What if a filter returns a wrong data type
    (array instead of string)?


12. What if two different plugins hook the same filter?


13. What if filter A runs first and filter B runs later,
    but filter B returns only the original content?


14. What if the filter hook is applied multiple times
    on the same page?


15. What if a filter calls apply_filters()
    for the SAME hook inside itself?


16. What if a filter is added dynamically
    inside another filter?


17. What if accepted_args is set incorrectly?


18. What if a filter modifies global variables?


19. What if the filter priority is very high (e.g. 999)?


20. What if remove_filter() is called
    with wrong priority or callback?


SECTION B: ACTION TESTCASES

21. What if two actions are hooked to the same action hook?


22. What if two actions have different priorities?


23. What if both actions echo output?


24. What if an action returns a value?


25. What if the same action function is added twice?


26. What if an action is removed BEFORE the hook runs?


27. What if an action is removed AFTER it already ran?


28. What if an action is added conditionally?


29. What if two plugins hook the same action?


30. What if an action triggers another action?


31. What if an action is added inside another action?


32. What if an action outputs HTML before headers are sent?


33. What if an action runs multiple times on one page?


34. What if an action has very high priority (e.g. 999)?


35. What if an action uses exit() or die()?


36. What if an action is added to the WRONG hook?


37. What if an action both echoes output
    and performs a redirect?


38. What if an action depends on data
    from another action?


39. What if an action is added in both
    theme and plugin?


40. What if add_action() is called inside a loop?


SECTION C: MIXED (FILTER + ACTION)

41. What if an action adds a filter dynamically?


42. What if a filter adds an action dynamically?


43. What if remove_action() is called inside a filter?


44. What if remove_filter() is called inside an action?


45. What if both filter and action hook
    the same hook name?


46. What if an action runs before a filter
    modifies the same data?


47. What if a filter depends on something
    created by an action?


48. What if priority conflicts between
    action and filter logic?


49. What if a filter modifies content
    but an action outputs HTML around it?


50. What if hooks are added after the hook
    has already fired?
